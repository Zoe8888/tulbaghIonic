import { ID } from '@datorama/akita';

export interface Tulbagh {
  id: ID;
  uniqueId: string;
  profile: string;
}

export function createTulbagh(params: Partial<Tulbagh>) {
  return {

  } as Tulbagh;
}
